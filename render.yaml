services:
  - type: web
    name: egret-backend
    env: python
    rootDir: backend
    buildCommand: pip install --upgrade pip && pip install --only-binary=:all: -r requirements.txt
    startCommand: gunicorn -k uvicorn.workers.UvicornWorker app.main:app --bind 0.0.0.0:$PORT
    autoDeploy: true
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./egret.db
      - key: REDIS_URL
        value: ""
      - key: CORS_ORIGINS
        value: https://egret-frontend.onrender.com,https://*.onrender.com
      - key: SECRET_KEY
        generateValue: true

  - type: web
    name: egret-frontend
    env: node
    rootDir: frontend
    buildCommand: npm install && npm run build
    startCommand: NODE_ENV=production next start -p $PORT
    autoDeploy: true
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://egret-3.onrender.com
